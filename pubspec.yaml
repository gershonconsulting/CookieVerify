name: linkedin_cookie_validator
description: LinkedIn Cookie Validator - Test and extract profile information from LinkedIn cookies
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: 1.5.0
  intl: ^0.19.0
  csv: ^6.0.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
